#include <stdio.h>

/**
  * main - Entry point of program
  * @argc: First argument
  * @argv: Second argument
  *
  * Return: 0 (Success)
  */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
