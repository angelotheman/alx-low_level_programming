#include <stdio.h>

/**
  * main - Entry point of the code, this function only prints the sentence
  *
  * Return : Always 0 (Success)
  */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
