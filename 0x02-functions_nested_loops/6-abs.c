#include "main.h"

/**
  * _abs - This function calculates the absolute value of digits.
  * @a: Value to be processed.
  *
  * Return: The absolute number of the integer.
  */

int _abs(int a)
{
	if (a < 0)
	{
		return (-a);
	}
	else
	{
		return (a);
	}
}
